# RosterIQ Agents (Python Lambda + LangGraph)

AI/ML agent workflows. Add LangGraph state machines and tool calls here.

- **Runtime:** Python 3.12
- **Deploy:** Built and deployed via CDK; dependencies from `requirements.txt`.
